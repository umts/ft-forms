!!!
%html
  %head
    %title Meet and Greet Request Form | UMass Transit | UMass Amherst
    = stylesheet_link_tag 'application.css', media: 'all'
    = javascript_include_tag 'application.js'
    = csrf_meta_tags
  %body
    #umass_banner
      #wordmark= image_tag 'wordmark.svg', height: 25
      #search= render partial: 'layouts/umass_search'
      #links
    #transportation_banner
      = image_tag 'trans-header-large.png'
    - if flash[:message].present?
      #message
        = flash[:message]
    - if flash[:errors].present?
      #errors
        The following errors occurred:
        %ul
          - flash[:errors].each do |error_message|
            %li= error_message
    #content_wrapper
      #main_content
        = yield
    #footer=render partial: 'layouts/footer'
